import { FormFieldValidationErrorPipe } from './form-field-validation-error.pipe';

describe('FormFieldValidationErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new FormFieldValidationErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
