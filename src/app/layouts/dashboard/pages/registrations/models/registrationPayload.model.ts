export interface IRegistrationPayload{
    turn: string | null,
    subject: string | null,
    hourFrom:string | null,
    hourTo: string | null,
    courseId: string | null,
    studentId: string | null 
}